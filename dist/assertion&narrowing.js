"use strict";
{
    // assertion
    let userName;
    userName = "halima";
    userName.includes("a");
    const myFunc = (num) => {
        if (typeof num === "number") {
            return num;
        }
    };
    let hh = myFunc(9);
    try {
    }
    catch (error) {
        console.log(error.message);
    }
    //
    //
}
